package com.github.beisser.design_patterns.factories;

/**
 * Abstract Factory
 *
 * if there is a hierarchy of objects there also needs to be the same hierarchy of factories
 * for those objects
 *
 * after that an factory can be build which uses methods with arguments managing the other factories
 * and therefore is abstract instead of concrete
 *
 */
public class AbstractFactory {

}
